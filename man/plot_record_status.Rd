% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_record_status.R
\name{plot_record_status}
\alias{plot_record_status}
\title{Plot Record Status Dashboard data}
\usage{
plot_record_status(
  data,
  record_id_field = NULL,
  mode = c("standard", "compact"),
  low_color = "#D73027",
  mid_color = "#FEE08B",
  high_color = "#1A9850",
  missing_color = "grey95",
  tile_color = "white",
  tile_linewidth = NULL,
  x_label = "REDCap Form",
  y_label = "Record ID",
  fill_label = "Completion Status",
  rotate_x_text = TRUE,
  x_text_size = NULL,
  y_text_size = NULL,
  x_tick_every = NULL,
  y_tick_every = NULL,
  show_y_title = NULL,
  form_label_max = NULL
)
}
\arguments{
\item{data}{A dataframe returned by \code{\link[=get_record_status_data]{get_record_status_data()}}.}

\item{record_id_field}{Optional string naming the record ID column. By
default, the function infers the only column other than \code{form_name},
\code{event_name}, and \code{pct_complete}.}

\item{mode}{Plot display mode. Use \code{"standard"} for the default dashboard
view or \code{"compact"} for smaller x-axis text, thinner tile borders, and
truncated long form labels. Compact mode preserves the default y-axis
labels and title unless those are overridden directly.}

\item{low_color}{Color for \code{pct_complete = 0}.}

\item{mid_color}{Color for \code{pct_complete = 0.5}.}

\item{high_color}{Color for \code{pct_complete = 1}.}

\item{missing_color}{Fill color for missing \code{pct_complete} values.}

\item{tile_color}{Border color for each dashboard tile.}

\item{tile_linewidth}{Border width for each dashboard tile. When \code{NULL},
the value is chosen from \code{mode}.}

\item{x_label}{X-axis label.}

\item{y_label}{Y-axis label.}

\item{fill_label}{Fill legend label.}

\item{rotate_x_text}{Whether to rotate form labels on the top axis.}

\item{x_text_size, y_text_size}{Optional axis text sizes.}

\item{x_tick_every, y_tick_every}{Optional positive integers for showing every
nth x- or y-axis tick label. When \code{NULL}, the axis is not thinned. These
arguments work in both standard and compact modes.}

\item{show_y_title}{Whether to show the y-axis title.}

\item{form_label_max}{Optional positive integer for truncating long form
labels. When \code{NULL}, compact mode truncates labels to 35 characters and
standard mode leaves labels unchanged.}
}
\value{
A ggplot object.
}
\description{
\code{plot_record_status()} creates a ggplot heat map from data returned by
\code{\link[=get_record_status_data]{get_record_status_data()}}. The plot is designed to resemble the REDCap
Record Status Dashboard while remaining a standard ggplot object that users
can extend with additional ggplot2 layers.
}
\details{
The function expects one row per dashboard tile with a \code{form_name} column and
a \code{pct_complete} column. The record ID column is inferred from the remaining
data columns when possible. For longitudinal output, \code{event_name} is ignored
because event labels are already included in \code{form_name}.

\code{pct_complete} should range from \code{0} to \code{1}, where \code{1} is complete and \code{0} is
incomplete or unverified. Missing values are shown with \code{missing_color}.
}
\examples{
plot_record_status(mock_record_status_data)

plot_record_status(mock_record_status_data, mode = "compact")

\dontrun{
data <- get_record_status_data(
  redcap_uri = Sys.getenv("REDCAP_URI"),
  token = Sys.getenv("REDCAP_TOKEN")
)

plot_record_status(data)
}

}
